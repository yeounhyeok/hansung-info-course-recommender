server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # For fast iteration: avoid aggressive caching
  add_header Cache-Control "no-store";

  location / {
    try_files $uri $uri/ =404;
  }
}
