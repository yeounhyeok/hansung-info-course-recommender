services:
  web:
    build: .
    ports:
      - "8080:80"
    # If you want hot-reload without rebuilding the image, uncomment volume mount:
    # volumes:
    #   - ./static:/usr/share/nginx/html:ro
